# Backend API для управления сущностями

Автоматически генерируемый REST API бэкенд на основе TypeScript интерфейсов из Vue компонентов.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и настройте параметры:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=test_entities_db
```

### 3. Генерация бэкенда

Запустите автоматическую генерацию всех компонентов:

```bash
npm run generate
```

Эта команда выполнит:
- ✅ Извлечение интерфейсов из Vue файлов
- ✅ Генерацию моделей (models/)
- ✅ Генерацию контроллеров (controllers/)
- ✅ Генерацию роутов (routes/)
- ✅ Генерацию Vue страниц с API интеграцией (src/pages/)

### 4. Инициализация базы данных

```bash
npm run init-db
```

### 5. Запуск сервера

**Development режим:**
```bash
npm run dev
```

**Production режим:**
```bash
npm run build
npm start
```

## 📁 Структура проекта

```
backend/
├── config/
│   └── db.js                 # Конфигурация БД
├── middleware/
│   └── errorHandler.js       # Обработка ошибок
├── models/                   # Модели данных (генерируются)
├── controllers/              # Контроллеры (генерируются)
├── routes/                   # Роуты (генерируются)
├── extract-interfaces.js     # Извлечение интерфейсов из Vue
├── generate-models.js        # Генератор моделей
├── generate-controllers.js   # Генератор контроллеров
├── generate-routes.js        # Генератор роутов
├── generate-vue-pages.js     # Генератор Vue страниц с API
├── generate-all.js           # Главный скрипт генерации
├── init-db.ts               # Инициализация БД
├── server.ts                # Главный файл сервера
└── entities-config.json     # Конфигурация сущностей
```

## 🛠️ Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск в dev режиме с hot reload |
| `npm run build` | Компиляция TypeScript в JavaScript |
| `npm start` | Запуск production сервера |
| `npm run generate` | Полная генерация бэкенда |
| `npm run extract` | Извлечение интерфейсов из Vue |
| `npm run generate:models` | Генерация только моделей |
| `npm run generate:controllers` | Генерация только контроллеров |
| `npm run generate:routes` | Генерация только роутов |
| `npm run generate:vue-pages` | Генерация только Vue страниц |
| `npm run init-db` | Инициализация базы данных |

## 📝 API Endpoints

Для каждой сущности автоматически создаются следующие endpoints:

### Пример для сущности `Priorities`:

- `GET /api/priorities` - Получить список с пагинацией и фильтрацией
- `GET /api/priorities/:id` - Получить по ID
- `POST /api/priorities` - Создать новый
- `PUT /api/priorities/:id` - Обновить существующий
- `DELETE /api/priorities/:id` - Удалить

### Query параметры для GET списка:

```
GET /api/priorities?q=search&sortBy=name&orderBy=asc&itemsPerPage=10&page=1
```

- `q` - поисковый запрос
- `sortBy` - поле для сортировки
- `orderBy` - направление сортировки (asc/desc)
- `itemsPerPage` - количество элементов на странице
- `page` - номер страницы

## 🔧 Как добавить новую сущность

1. Создайте Vue компонент с интерфейсом в `src/pages/apps/settings/`
2. Запустите генерацию: `npm run generate`
3. Запустите инициализацию БД: `npm run init-db`
4. Перезапустите сервер

## 🎯 Пример интерфейса в Vue компоненте

```typescript
interface Priority {
  id: number
  name: string
  color: string
  createdAt: string
  updatedAt: string
  status: number // 1 - активен, 2 - не активен
  isActive: boolean
}
```

Генератор автоматически:
- Создаст модель с CRUD операциями
- Создаст контроллер с валидацией
- Создаст роуты
- Создаст таблицу в БД

## 🔐 Безопасность

- ✅ Параметризованные SQL запросы (защита от SQL injection)
- ✅ Валидация входных данных
- ✅ Обработка ошибок через middleware
- ✅ CORS настройки
- ⚠️ TODO: Добавить JWT аутентификацию
- ⚠️ TODO: Добавить rate limiting

## 📊 База данных

Используется PostgreSQL. Каждая таблица имеет стандартные поля:

- `id` - PRIMARY KEY, SERIAL
- `created_at` - TIMESTAMP
- `updated_at` - TIMESTAMP
- `status` - INTEGER (1 - активен, 2 - не активен)
- `is_active` - BOOLEAN

## 🐛 Отладка

Логи сервера выводятся в консоль. В development режиме включено подробное логирование:

```bash
npm run dev
```

Проверка здоровья сервера:

```bash
curl http://localhost:3000/health
```

## 📚 Технологии

- **Node.js** - Runtime
- **Express** - Web framework
- **TypeScript** - Типизация
- **PostgreSQL** - База данных
- **pg** - PostgreSQL клиент
- **dotenv** - Управление переменными окружения
- **cors** - CORS middleware

## 🤝 Вклад в проект

При изменении генераторов:

1. Тестируйте на нескольких сущностях
2. Проверяйте сгенерированный код
3. Обновляйте документацию

## 📄 Лицензия

ISC
